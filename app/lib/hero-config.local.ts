import { HeroConfig } from "@/app/types/heroConfig";

export const heroConfig: HeroConfig = {
  autoplay: true,
  intervalMs: 6000,
  transitionMs: 700,
  pauseOnHover: true,
  showControls: true,
  showDots: true,
};
